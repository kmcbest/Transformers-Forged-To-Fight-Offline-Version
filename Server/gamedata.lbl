use jsonout

record RosterEntry
  bid: text
  faction: text
  klass: text
  star: integer
end

record TextEntry
  key: text
  value: text
end

record Stats
  hp: integer
  atk: integer
end

record QuestEncounter
  key: text
  final_boss: boolean
  label: text
end

record BuildingSpec
  bid: text
  name: text
  desc: text
  model: text
end

record BasePlacement
  row: integer
  col: integer
  bid: text
end

function last_slash(path: text): integer
  intent: find
  mutable result: integer = -1
  mutable index: integer = 0
  while index < text_length(path) do
    if substring(path, index, 1) == "/" then
  set result = index
end
    set index = index + 1
  end
  result
end

function directory_name(path: text): text
  intent: get
  let slash: integer = last_slash(path)
  if slash < 0 then
  "."
else
  substring(path, 0, slash)
end
end

function absolute_script_path(arguments: a list of text): text
  intent: return
  let path: text = unwrap_or(get(arguments, 0), "")
  if starts_with(path, "/") then
  path
else
  path_join(get_cwd(), path)
end
end

public function here(): text
  intent: get
  directory_name(absolute_script_path(get_args()))
end

public function resp_dir(): text
  intent: get
  path_join(here(), "responses")
end

public function diag_mana_start(): integer
  intent: get
  max(0, min(1, to_integer(unwrap_or(env_get("TFTF_DIAG_MANA_START"), "0"))))
end

public function mana_gain_rate(): decimal
  intent: return
  1
end

public function classes(): a list of text
  intent: return
  ["braw", "tact", "scou", "demo", "warr", "tech"]
end

public function cdn(): text
  intent: return
  "https://tform-0901-hzlhiniyfcwf.tf-cdn.net"
end

function roster_part_1(): a list of RosterEntry
  intent: return
  [RosterEntry { bid: "fte_optimus_gs_t3", faction: "autobot", klass: "tact", star: 3 }, RosterEntry { bid: "fte_stars_gs_t3", faction: "decepticon", klass: "warr", star: 3 }, RosterEntry { bid: "arcee_gs_deluxe2014", faction: "autobot", klass: "scou", star: 3 }, RosterEntry { bid: "blaster_gs_leader2016", faction: "autobot", klass: "tech", star: 4 }, RosterEntry { bid: "bumblebee_cin_dotm", faction: "autobot", klass: "scou", star: 3 }, RosterEntry { bid: "bumblebee_gs_kabam", faction: "autobot", klass: "scou", star: 3 }, RosterEntry { bid: "cheetor_bw_transmetal", faction: "autobot", klass: "scou", star: 4 }, RosterEntry { bid: "cliffjumper_gs_kabam", faction: "autobot", klass: "warr", star: 2 }, RosterEntry { bid: "dinobot_bw_kabam", faction: "autobot", klass: "warr", star: 4 }, RosterEntry { bid: "drift_cin_aoe", faction: "autobot", klass: "tact", star: 4 }, RosterEntry { bid: "grimlock_gs_mp08", faction: "autobot", klass: "braw", star: 5 }, RosterEntry { bid: "hotrod_cin_tlk", faction: "autobot", klass: "warr", star: 4 }, RosterEntry { bid: "hound_cin_tlk", faction: "autobot", klass: "demo", star: 3 }, RosterEntry { bid: "ironhide_cin_rotf", faction: "autobot", klass: "tact", star: 3 }, RosterEntry { bid: "ironhide_gs_kabam", faction: "autobot", klass: "tact", star: 3 }, RosterEntry { bid: "jazz_gs_twm05", faction: "autobot", klass: "scou", star: 3 }, RosterEntry { bid: "jetfire_gs_leader2014", faction: "autobot", klass: "tech", star: 4 }, RosterEntry { bid: "mirage_gs_deluxe2016", faction: "autobot", klass: "scou", star: 3 }, RosterEntry { bid: "optimusprimal_bw_mp32", faction: "autobot", klass: "braw", star: 5 }, RosterEntry { bid: "optimusprime_cin_tf", faction: "autobot", klass: "braw", star: 5 }, RosterEntry { bid: "prowl_gs_deluxe2016", faction: "autobot", klass: "tact", star: 3 }]
end

function roster_part_2(): a list of RosterEntry
  intent: return
  [RosterEntry { bid: "ratchet_gs_kabam", faction: "autobot", klass: "tech", star: 2 }, RosterEntry { bid: "rhinox_gs_voyager2014", faction: "autobot", klass: "braw", star: 3 }, RosterEntry { bid: "rodimusprime_gs_mp09", faction: "autobot", klass: "warr", star: 4 }, RosterEntry { bid: "sideswipe_gs", faction: "autobot", klass: "warr", star: 3 }, RosterEntry { bid: "sunstreaker_gs_deluxe2008", faction: "autobot", klass: "warr", star: 3 }, RosterEntry { bid: "ultramagnus_gs_leader", faction: "autobot", klass: "braw", star: 4 }, RosterEntry { bid: "wheeljack_gs_mp20", faction: "autobot", klass: "tech", star: 3 }, RosterEntry { bid: "windblade_gs", faction: "autobot", klass: "scou", star: 4 }, RosterEntry { bid: "barricade_cin_dotm", faction: "decepticon", klass: "scou", star: 3 }, RosterEntry { bid: "bludgeon_gs_rd20", faction: "decepticon", klass: "warr", star: 4 }, RosterEntry { bid: "bonecrusher_cin_rotf", faction: "decepticon", klass: "demo", star: 3 }, RosterEntry { bid: "cyclonus_gs_uw06", faction: "decepticon", klass: "warr", star: 3 }, RosterEntry { bid: "dirge_gs_deluxe2008", faction: "decepticon", klass: "warr", star: 2 }, RosterEntry { bid: "galvatron_gs_voyager2016", faction: "decepticon", klass: "braw", star: 5 }, RosterEntry { bid: "grindor_cin_rotf", faction: "decepticon", klass: "demo", star: 3 }, RosterEntry { bid: "kickback_gs_kabam", faction: "decepticon", klass: "scou", star: 2 }, RosterEntry { bid: "megatron_cin_rotf", faction: "decepticon", klass: "braw", star: 5 }, RosterEntry { bid: "megatron_gs_leader2015", faction: "decepticon", klass: "braw", star: 5 }, RosterEntry { bid: "megatronus_gs_kabam", faction: "decepticon", klass: "tact", star: 5 }, RosterEntry { bid: "mixmaster_cin_rotf", faction: "decepticon", klass: "demo", star: 3 }, RosterEntry { bid: "motormaster_gs_voyager2015", faction: "decepticon", klass: "braw", star: 4 }]
end

function roster_part_3(): a list of RosterEntry
  intent: return
  [RosterEntry { bid: "necrotronus_gs_kabam", faction: "decepticon", klass: "demo", star: 4 }, RosterEntry { bid: "nemesisprime_gs_voyager2015", faction: "decepticon", klass: "tact", star: 4 }, RosterEntry { bid: "ramjet_gs_deluxe2008", faction: "decepticon", klass: "warr", star: 2 }, RosterEntry { bid: "scorponok_bw_kabam", faction: "decepticon", klass: "tech", star: 3 }, RosterEntry { bid: "shockwave_gs", faction: "decepticon", klass: "tech", star: 4 }, RosterEntry { bid: "skywarp_gs_leader2015", faction: "decepticon", klass: "warr", star: 4 }, RosterEntry { bid: "slipstream_gs", faction: "decepticon", klass: "tact", star: 3 }, RosterEntry { bid: "soundblaster_gs_mp13b", faction: "decepticon", klass: "tech", star: 4 }, RosterEntry { bid: "soundwave_gs", faction: "decepticon", klass: "tech", star: 4 }, RosterEntry { bid: "thundercracker_gs_leader2015", faction: "decepticon", klass: "warr", star: 4 }, RosterEntry { bid: "tantrum_gs_kabam", faction: "decepticon", klass: "braw", star: 2 }, RosterEntry { bid: "waspinator_gs_deluxe", faction: "decepticon", klass: "tech", star: 2 }, RosterEntry { bid: "sharkticon_gs_kabam", faction: "decepticon", klass: "braw", star: 1 }, RosterEntry { bid: "sharkticon_gs_brawler", faction: "decepticon", klass: "braw", star: 1 }, RosterEntry { bid: "sharkticon_gs_demolition", faction: "decepticon", klass: "demo", star: 1 }, RosterEntry { bid: "sharkticon_gs_scout", faction: "decepticon", klass: "scou", star: 1 }, RosterEntry { bid: "sharkticon_gs_tactician", faction: "decepticon", klass: "tact", star: 1 }, RosterEntry { bid: "sharkticon_gs_tech", faction: "decepticon", klass: "tech", star: 1 }, RosterEntry { bid: "sharkticon_gs_warrior", faction: "decepticon", klass: "warr", star: 1 }]
end

public function roster(): a list of RosterEntry
  intent: build
  mutable result: a list of RosterEntry = []
  for entry in roster_part_1() do
    set result = append(result, entry)
  end
  for entry in roster_part_2() do
    set result = append(result, entry)
  end
  for entry in roster_part_3() do
    set result = append(result, entry)
  end
  result
end

public function owned(): a list of text
  intent: build
  mutable result: a list of text = []
  for entry in roster() do
    set result = append(result, entry.bid)
  end
  result
end

function class_hp_mult(klass: text): decimal
  intent: return
  if klass == "braw" then
  1.25
else if klass == "scou" then
  0.85
else if klass == "demo" then
  1.1
else if klass == "warr" then
  0.95
else if klass == "tech" then
  1.05
else
  1
end
end

function class_atk_mult(klass: text): decimal
  intent: return
  if klass == "braw" then
  0.9
else if klass == "scou" then
  1.15
else if klass == "demo" then
  1.05
else if klass == "warr" then
  1.2
else if klass == "tech" then
  0.95
else
  1
end
end

function star_hp(star: integer): integer
  intent: return
  if star == 2 then
  3400
else if star == 3 then
  5200
else if star == 4 then
  7800
else if star == 5 then
  11500
else
  2200
end
end

function star_atk(star: integer): integer
  intent: return
  if star == 2 then
  300
else if star == 3 then
  430
else if star == 4 then
  610
else if star == 5 then
  850
else
  210
end
end

function roster_entry(bid: text): RosterEntry
  intent: find
  mutable result: RosterEntry = RosterEntry { bid: bid, faction: "decepticon", klass: "tact", star: 1 }
  for entry in roster() do
    if entry.bid == bid then
  set result = entry
end
  end
  result
end

public function base_stats(bid: text, rank: integer, level: integer): Stats
  intent: build
  let entry: RosterEntry = roster_entry(bid)
  let rank_mult: decimal = 1 + 0.35 * to_decimal(max(1, rank) - 1)
  let level_add: decimal = to_decimal(max(1, level) - 1) / 100
  let level_factor: decimal = 1 + level_add
  let factor: decimal = rank_mult * level_factor
  Stats { hp: to_integer(to_decimal(star_hp(entry.star)) * class_hp_mult(entry.klass) * factor), atk: to_integer(to_decimal(star_atk(entry.star)) * class_atk_mult(entry.klass) * factor) }
end

public function max_special_attacks(bid: text, star: integer): integer
  intent: return
  -- Rarity still drives stats and presentation, but this preservation build has no
  -- functional rank-up path, so every bot exposes its three authored attacks immediately.
  3
end

public function special_damage_ratios(bid: text, star: integer): a list of decimal
  intent: return
  [1.75, 2.5, 3.5]
end

public function art_overrides(): a list of TextEntry
  intent: return
  [TextEntry { key: "fte_optimus_gs_t3", value: "optimus_gs" }, TextEntry { key: "fte_stars_gs_t3", value: "stars_gs" }, TextEntry { key: "blaster_gs_leader2016", value: "blastr_gs" }, TextEntry { key: "bumblebee_cin_dotm", value: "bumbl_c" }, TextEntry { key: "bumblebee_gs_kabam", value: "bumbl_gs" }, TextEntry { key: "cliffjumper_gs_kabam", value: "cliffjump_gs" }, TextEntry { key: "dinobot_bw_kabam", value: "dinob_bw" }, TextEntry { key: "drift_cin_aoe", value: "drift_c" }, TextEntry { key: "grimlock_gs_mp08", value: "griml_gs" }, TextEntry { key: "hotrod_cin_tlk", value: "hotrod_c" }, TextEntry { key: "hound_cin_tlk", value: "hound_c" }, TextEntry { key: "ironhide_cin_rotf", value: "ironh_c_rotf" }, TextEntry { key: "ironhide_gs_kabam", value: "ironh_gs" }, TextEntry { key: "mirage_gs_deluxe2016", value: "mirag_gs" }, TextEntry { key: "optimusprimal_bw_mp32", value: "oprimal_bw" }, TextEntry { key: "optimusprime_cin_tf", value: "optimus_c_tf" }, TextEntry { key: "ratchet_gs_kabam", value: "ratch_gs" }, TextEntry { key: "rhinox_gs_voyager2014", value: "rhino_bw" }, TextEntry { key: "rodimusprime_gs_mp09", value: "rodimus_gs" }, TextEntry { key: "sideswipe_gs", value: "sides_gs" }, TextEntry { key: "sunstreaker_gs_deluxe2008", value: "sunstreak_gs" }, TextEntry { key: "ultramagnus_gs_leader", value: "ultram_gs" }, TextEntry { key: "wheeljack_gs_mp20", value: "wheelj_gs" }, TextEntry { key: "windblade_gs", value: "windb_gs" }, TextEntry { key: "barricade_cin_dotm", value: "barri_c" }, TextEntry { key: "bludgeon_gs_rd20", value: "bludge_gs" }, TextEntry { key: "bonecrusher_cin_rotf", value: "bonec_c" }, TextEntry { key: "cyclonus_gs_uw06", value: "cyclon_gs" }, TextEntry { key: "grindor_cin_rotf", value: "grind_c_rotf" }, TextEntry { key: "kickback_gs_kabam", value: "kickb_gs" }, TextEntry { key: "megatron_cin_rotf", value: "megat_c" }, TextEntry { key: "megatron_gs_leader2015", value: "megat_gs" }, TextEntry { key: "megatronus_gs_kabam", value: "megatro_gs" }, TextEntry { key: "mixmaster_cin_rotf", value: "mixma_c_rotf" }, TextEntry { key: "motormaster_gs_voyager2015", value: "motorm_gs" }, TextEntry { key: "necrotronus_gs_kabam", value: "necrotro_gs" }, TextEntry { key: "nemesisprime_gs_voyager2015", value: "nemesis_p" }, TextEntry { key: "shockwave_gs", value: "shock_c" }, TextEntry { key: "soundblaster_gs_mp13b", value: "soundblast_gs" }, TextEntry { key: "soundwave_gs", value: "sound_gs" }, TextEntry { key: "thundercracker_gs_leader2015", value: "thunder_gs" }, TextEntry { key: "waspinator_gs_deluxe", value: "wasp_bw" }, TextEntry { key: "sharkticon_gs_kabam", value: "npc_shark_gold" }, TextEntry { key: "sharkticon_gs_brawler", value: "npc_shark_braw" }, TextEntry { key: "sharkticon_gs_demolition", value: "npc_shark_demo" }, TextEntry { key: "sharkticon_gs_scout", value: "npc_shark_scou" }, TextEntry { key: "sharkticon_gs_tactician", value: "npc_shark_tact" }, TextEntry { key: "sharkticon_gs_tech", value: "npc_shark_tech" }, TextEntry { key: "sharkticon_gs_warrior", value: "npc_shark_warr" }]
end

public function art_base(bid: text): text
  intent: find
  mutable result: text = ""
  mutable found: boolean = false
  for entry in art_overrides() do
    if entry.key == bid then
  set result = entry.value
  set found = true
end
  end
  if found then
  result
else
  let parts: a list of text = split(bid, "_")
  if length(parts) >= 2 then
  unwrap_or(get(parts, 0), "") ++ "_" ++ unwrap_or(get(parts, 1), "")
else
  bid
end
end
end

public function model_overrides(): a list of TextEntry
  intent: return
  [TextEntry { key: "fte_optimus_gs_t3", value: "optimusprime_cin_tf" }, TextEntry { key: "fte_stars_gs_t3", value: "skywarp_gs_leader2015" }]
end

public function model_id(bid: text): text
  intent: find
  mutable result: text = bid
  for entry in model_overrides() do
    if entry.key == bid then
  set result = entry.value
end
  end
  result
end

public function display_name(bid: text): text
  intent: build
  let parts: a list of text = split(bid, "_")
  mutable token: text = unwrap_or(get(parts, 0), "")
  if token == "fte" and length(parts) > 1 then
  set token = unwrap_or(get(parts, 1), "")
end
  uppercase(substring(token, 0, 1)) ++ substring(token, 1, text_length(token) - 1)
end

public function build_blueprints(): Json
  intent: build
  mutable output: a list of Pair = []
  for entry in roster() do
    let ratios: a list of decimal = special_damage_ratios(entry.bid, entry.star)
    let model: text = model_id(entry.bid)
    let art: text = art_base(entry.bid)
    let name: text = display_name(entry.bid)
    let blueprint: Json = jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text(entry.bid)), jsonout.json_pair("et", jsonout.json_text("bot")), jsonout.json_pair("c", jsonout.json_text(entry.bid)), jsonout.json_pair("r", jsonout.json_int(entry.star)), jsonout.json_pair("a", jsonout.json_text(entry.faction)), jsonout.json_pair("cl", jsonout.json_text(entry.klass)), jsonout.json_pair("name", jsonout.json_text(name)), jsonout.json_pair("name_s", jsonout.json_text(name)), jsonout.json_pair("m", jsonout.json_text(model)), jsonout.json_pair("mdl", jsonout.json_text(model)), jsonout.json_pair("i", jsonout.json_text(art)), jsonout.json_pair("img", jsonout.json_text(art)), jsonout.json_pair("ma", jsonout.json_text(model)), jsonout.json_pair("map_asset", jsonout.json_text(model)), jsonout.json_pair("s1", jsonout.json_dec(unwrap_or(get(ratios, 0), 0))), jsonout.json_pair("s2", jsonout.json_dec(unwrap_or(get(ratios, 1), 0))), jsonout.json_pair("s3", jsonout.json_dec(unwrap_or(get(ratios, 2), 0))), jsonout.json_pair("msa", jsonout.json_int(max_special_attacks(entry.bid, entry.star))), jsonout.json_pair("ab", jsonout.json_dec(100)), jsonout.json_pair("gg", jsonout.json_int(1)), jsonout.json_pair("mfl", jsonout.json_int(0)), jsonout.json_pair("nfr", jsonout.json_int(0)), jsonout.json_pair("fcpg", jsonout.json_text("")), jsonout.json_pair("fhpag", jsonout.json_text(""))])
    set output = append(output, jsonout.json_pair(entry.bid, blueprint))
  end
  jsonout.json_obj(output)
end

function dump_helpers(): nothing
  intent: print
  for entry in roster() do
    for rank in [1, 2, 3, 5] do
      for level in [1, 7, 30] do
        let stats: Stats = base_stats(entry.bid, rank, level)
        print(entry.bid ++ " " ++ to_text(rank) ++ " " ++ to_text(level) ++ " " ++ to_text(stats.hp) ++ " " ++ to_text(stats.atk))
      end
    end
    print("MSA " ++ entry.bid ++ " " ++ to_text(max_special_attacks(entry.bid, entry.star)))
    print("NAME " ++ entry.bid ++ " " ++ display_name(entry.bid))
    print("ART " ++ entry.bid ++ " " ++ art_base(entry.bid))
    print("MODEL " ++ entry.bid ++ " " ++ model_id(entry.bid))
  end
  let unknown: Stats = base_stats("no_such_bot_xyz", 1, 1)
  let ids: a list of text = owned()
  print("UNKNOWN (" ++ to_text(unknown.hp) ++ ", " ++ to_text(unknown.atk) ++ ")")
  print("OWNED " ++ to_text(length(ids)) ++ " " ++ unwrap_or(get(ids, 0), "") ++ " " ++ unwrap_or(get(ids, length(ids) - 1), ""))
  print("CDN " ++ cdn())
  print("CLASSES " ++ join(classes(), ","))
  print("DIAG " ++ to_text(diag_mana_start()))
end

public function build_characters(): Json
  intent: build
  mutable output: a list of Pair = []
  for entry in roster() do
    let base: text = art_base(entry.bid)
    let name: text = display_name(entry.bid)
    let model: text = model_id(entry.bid)
    let character: Json = jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text(entry.bid)), jsonout.json_pair("name", jsonout.json_text(name)), jsonout.json_pair("name_s", jsonout.json_text(name)), jsonout.json_pair("sg", jsonout.json_text("")), jsonout.json_pair("gen", jsonout.json_text(entry.faction)), jsonout.json_pair("aip", jsonout.json_text("")), jsonout.json_pair("sps", jsonout.json_text("")), jsonout.json_pair("i", jsonout.json_text(base)), jsonout.json_pair("img", jsonout.json_text(base)), jsonout.json_pair("m", jsonout.json_text(model)), jsonout.json_pair("mdl", jsonout.json_text(model)), jsonout.json_pair("ma", jsonout.json_text(model)), jsonout.json_pair("map_asset", jsonout.json_text(model)), jsonout.json_pair("hc", jsonout.json_text(entry.faction)), jsonout.json_pair("hero_colour", jsonout.json_text(entry.faction))])
    set output = append(output, jsonout.json_pair(entry.bid, character))
  end
  jsonout.json_obj(output)
end

function attack_value(attack_id: text, percent: decimal, mana: decimal, chance: decimal, damage: decimal, pierce: decimal): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text(attack_id)), jsonout.json_pair("a", jsonout.json_dec(percent)), jsonout.json_pair("m", jsonout.json_dec(mana)), jsonout.json_pair("c", jsonout.json_dec(chance)), jsonout.json_pair("d", jsonout.json_dec(damage)), jsonout.json_pair("p", jsonout.json_dec(pierce))])
end

public function build_attack_values(): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("Light", attack_value("Light", 0.35, 50, 0.05, 1.5, 0)), jsonout.json_pair("Medium", attack_value("Medium", 0.6, 75, 0.05, 1.5, 0)), jsonout.json_pair("Heavy", attack_value("Heavy", 1, 120, 0.05, 1.5, 0.05)), jsonout.json_pair("Ranged", attack_value("Ranged", 0.4, 55, 0.05, 1.5, 0))])
end

public function build_missions_config(): Json
  intent: build
  let combat: Json = jsonout.json_obj([jsonout.json_pair("armorRatingConstant", jsonout.json_dec(1000)), jsonout.json_pair("manaPerSpecial", jsonout.json_dec(300)), jsonout.json_pair("maxQueuedActionTime", jsonout.json_dec(0.2))])
  let user_base: Json = jsonout.json_obj([jsonout.json_pair("minClaimInterval", jsonout.json_int(60)), jsonout.json_pair("minClaimPercentage", jsonout.json_dec(0.1))])
  jsonout.json_obj([jsonout.json_pair("configsHash", jsonout.json_text("offline-v1")), jsonout.json_pair("configs", jsonout.json_obj([jsonout.json_pair("bcg-combat", combat), jsonout.json_pair("user-base", user_base)]))])
end

public function build_missions_autorefresh_result(): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("check", jsonout.json_text("offline-v1")), jsonout.json_pair("refresh", jsonout.json_int(0)), jsonout.json_pair("cache", jsonout.json_bool(false)), jsonout.json_pair("missionsconfig", build_missions_config())])
end

public function build_missions_autorefresh_update(): Json
  intent: build
  let result: Json = build_missions_autorefresh_result()
  jsonout.json_obj([jsonout.json_pair("name", jsonout.json_text("missionsconfig")), jsonout.json_pair("error", jsonout.json_text("")), jsonout.json_pair("check", jsonout.obj_get(result, "check")), jsonout.json_pair("locHash", jsonout.json_text("")), jsonout.json_pair("refresh", jsonout.obj_get(result, "refresh")), jsonout.json_pair("data", jsonout.obj_get(result, "missionsconfig")), jsonout.json_pair("cache", jsonout.obj_get(result, "cache"))])
end

public function build_missions_account_data(): Json
  intent: build
  let config: Json = build_missions_config()
  let result: Json = build_missions_autorefresh_result()
  mutable account: Json = config
  set account = jsonout.obj_set(account, "check", jsonout.obj_get(result, "check"))
  set account = jsonout.obj_set(account, "refresh", jsonout.obj_get(result, "refresh"))
  set account = jsonout.obj_set(account, "cache", jsonout.obj_get(result, "cache"))
  set account = jsonout.obj_set(account, "missionsconfig", jsonout.obj_get(result, "missionsconfig"))
  account
end

function class_names(): a list of TextEntry
  intent: return
  [TextEntry { key: "braw", value: "Brawler" }, TextEntry { key: "tact", value: "Tactician" }, TextEntry { key: "scou", value: "Scout" }, TextEntry { key: "demo", value: "Demolitions" }, TextEntry { key: "warr", value: "Warrior" }, TextEntry { key: "tech", value: "Tech" }]
end

function class_ring(): a list of text
  intent: return
  ["braw", "tact", "scou", "demo", "warr", "tech"]
end

function class_name(cid: text): text
  intent: find
  mutable result: text = ""
  for entry in class_names() do
    if entry.key == cid then
  set result = entry.value
end
  end
  result
end

function next_class(ring: a list of text, index: integer): text
  intent: find
  if index == length(ring) - 1 then
  unwrap_or(get(ring, 0), "")
else
  unwrap_or(get(ring, index + 1), "")
end
end

public function build_hero_classes(): Json
  intent: build
  let ring: a list of text = class_ring()
  mutable output: a list of Pair = []
  mutable index: integer = 0
  for cid in ring do
    let beats: text = next_class(ring, index)
    let name: text = class_name(cid)
    let row: Json = jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text(cid)), jsonout.json_pair("cid", jsonout.json_text(cid)), jsonout.json_pair("class_id", jsonout.json_text(cid)), jsonout.json_pair("n", jsonout.json_text(name)), jsonout.json_pair("name", jsonout.json_text(name)), jsonout.json_pair("class_name", jsonout.json_text(name)), jsonout.json_pair("ab", jsonout.json_dec(1.1)), jsonout.json_pair("atkb", jsonout.json_dec(1.1)), jsonout.json_pair("attack_bonus", jsonout.json_dec(1.1)), jsonout.json_pair("atkp", jsonout.json_dec(0.9)), jsonout.json_pair("attack_penalty", jsonout.json_dec(0.9)), jsonout.json_pair("ic", jsonout.json_list([jsonout.json_text(beats)])), jsonout.json_pair("ideal_contender", jsonout.json_list([jsonout.json_text(beats)]))])
    set output = append(output, jsonout.json_pair(cid, row))
    set index = index + 1
  end
  jsonout.json_obj(output)
end

public function build_rarity_properties(): Json
  intent: build
  mutable output: a list of Pair = []
  for star in [1, 2, 3, 4, 5] do
    let key: text = to_text(star)
    let name: text = key ++ " Star"
    let row: Json = jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text(key)), jsonout.json_pair("n", jsonout.json_text(name)), jsonout.json_pair("name", jsonout.json_text(name)), jsonout.json_pair("mv", jsonout.json_int(star)), jsonout.json_pair("map_value", jsonout.json_int(star)), jsonout.json_pair("sp3qt", jsonout.json_dec(0.5)), jsonout.json_pair("sp3_quicktime", jsonout.json_dec(0.5)), jsonout.json_pair("ms", jsonout.json_int(99)), jsonout.json_pair("max_sig", jsonout.json_int(99))])
    set output = append(output, jsonout.json_pair(key, row))
  end
  jsonout.json_obj(output)
end

function rating_for(hp: integer, atk: integer): integer
  intent: build
  let total: integer = hp + atk
  total / 20
end

public function build_hero_base(bid: text, rank: integer): Json
  intent: build
  let entry: RosterEntry = roster_entry(bid)
  let stats: Stats = base_stats(bid, rank, 1)
  let rating: integer = rating_for(stats.hp, stats.atk)
  jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text(bid)), jsonout.json_pair("r", jsonout.json_int(rank)), jsonout.json_pair("m", jsonout.json_int(entry.star)), jsonout.json_pair("s", jsonout.json_int(entry.star)), jsonout.json_pair("max_hp", jsonout.json_int(stats.hp)), jsonout.json_pair("mhpb", jsonout.json_int(stats.hp)), jsonout.json_pair("attack", jsonout.json_int(stats.atk)), jsonout.json_pair("attb", jsonout.json_int(stats.atk)), jsonout.json_pair("mana_start", jsonout.json_int(diag_mana_start())), jsonout.json_pair("stun_time", jsonout.json_int(0)), jsonout.json_pair("special_attacks", jsonout.json_int(max_special_attacks(bid, entry.star))), jsonout.json_pair("rating", jsonout.json_int(rating)), jsonout.json_pair("rating_hp", jsonout.json_int(stats.hp / 2)), jsonout.json_pair("rating_attack", jsonout.json_int(stats.atk / 2)), jsonout.json_pair("rating_hp_base", jsonout.json_int(stats.hp / 2)), jsonout.json_pair("rating_attack_base", jsonout.json_int(stats.atk / 2)), jsonout.json_pair("ab", jsonout.json_int(0)), jsonout.json_pair("hp", jsonout.json_dec(to_decimal(stats.hp))), jsonout.json_pair("armor", jsonout.json_dec(0)), jsonout.json_pair("crit_chance", jsonout.json_dec(0.05)), jsonout.json_pair("crit_damage", jsonout.json_dec(1.5)), jsonout.json_pair("perfect_block_chance", jsonout.json_dec(0.1)), jsonout.json_pair("block_proficiency", jsonout.json_dec(0.75)), jsonout.json_pair("mana_gain", jsonout.json_dec(mana_gain_rate())), jsonout.json_pair("resist_magic", jsonout.json_dec(0)), jsonout.json_pair("resist_physical", jsonout.json_dec(0)), jsonout.json_pair("stun_chance", jsonout.json_dec(0.05)), jsonout.json_pair("cr", jsonout.json_dec(0)), jsonout.json_pair("rcr", jsonout.json_dec(0)), jsonout.json_pair("rcd", jsonout.json_dec(0)), jsonout.json_pair("spb", jsonout.json_dec(0)), jsonout.json_pair("pjb", jsonout.json_dec(0)), jsonout.json_pair("cpw", jsonout.json_dec(0)), jsonout.json_pair("ap", jsonout.json_dec(0)), jsonout.json_pair("bp", jsonout.json_dec(0)), jsonout.json_pair("il", jsonout.json_dec(0)), jsonout.json_pair("il2", jsonout.json_dec(0)), jsonout.json_pair("il3", jsonout.json_dec(0)), jsonout.json_pair("is4", jsonout.json_dec(0)), jsonout.json_pair("eg", jsonout.json_dec(0)), jsonout.json_pair("fg", jsonout.json_dec(0)), jsonout.json_pair("ar", jsonout.json_dec(0)), jsonout.json_pair("hr", jsonout.json_dec(0)), jsonout.json_pair("hm", jsonout.json_dec(0)), jsonout.json_pair("am", jsonout.json_dec(0)), jsonout.json_pair("hrhp", jsonout.json_dec(0)), jsonout.json_pair("hra", jsonout.json_dec(0)), jsonout.json_pair("stat_mods", jsonout.json_list([])), jsonout.json_pair("sig_mods", jsonout.json_list([])), jsonout.json_pair("buff_mods", jsonout.json_list([])), jsonout.json_pair("i", jsonout.json_list([])), jsonout.json_pair("i2", jsonout.json_list([])), jsonout.json_pair("i3", jsonout.json_list([])), jsonout.json_pair("i4", jsonout.json_list([]))])
end

public function build_heroes(): Json
  intent: build
  mutable output: a list of Pair = []
  for bid in owned() do
    set output = append(output, jsonout.json_pair(bid, jsonout.json_obj([jsonout.json_pair("1", build_hero_base(bid, 1))])))
  end
  jsonout.json_obj(output)
end

public function build_stat_modifiers(): Json
  intent: build
  let row: Json = jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text("gp_hit_stun")), jsonout.json_pair("t", jsonout.json_text("hit_stun")), jsonout.json_pair("tm", jsonout.json_text("")), jsonout.json_pair("tr", jsonout.json_list([])), jsonout.json_pair("uit", jsonout.json_list([])), jsonout.json_pair("pri", jsonout.json_int(0)), jsonout.json_pair("trm", jsonout.json_dec(0)), jsonout.json_pair("trs", jsonout.json_text("")), jsonout.json_pair("trr", jsonout.json_text("none")), jsonout.json_pair("c", jsonout.json_dec(1)), jsonout.json_pair("m", jsonout.json_dec(1)), jsonout.json_pair("d", jsonout.json_dec(0.5)), jsonout.json_pair("s", jsonout.json_text("none")), jsonout.json_pair("ta", jsonout.json_text("self")), jsonout.json_pair("mt", jsonout.json_text("debuff")), jsonout.json_pair("v", jsonout.json_text("")), jsonout.json_pair("ms", jsonout.json_text("")), jsonout.json_pair("st", jsonout.json_int(0)), jsonout.json_pair("g", jsonout.json_text("")), jsonout.json_pair("gc", jsonout.json_dec(0)), jsonout.json_pair("gcv", jsonout.json_text("")), jsonout.json_pair("rcv", jsonout.json_text("")), jsonout.json_pair("ti", jsonout.json_int(0)), jsonout.json_pair("a", jsonout.json_list([])), jsonout.json_pair("au", jsonout.json_list([])), jsonout.json_pair("rh", jsonout.json_dec(0)), jsonout.json_pair("ra", jsonout.json_dec(0))])
  jsonout.json_obj([jsonout.json_pair("gp_hit_stun", row)])
end

public function build_login_data(): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("cdn", jsonout.json_text(cdn())), jsonout.json_pair("sigLvlMax", jsonout.json_int(99)), jsonout.json_pair("ratingPrecision", jsonout.json_int(4)), jsonout.json_pair("heroRatingAttackWeight", jsonout.json_dec(1)), jsonout.json_pair("heroRatingMaxHPWeight", jsonout.json_dec(1)), jsonout.json_pair("attributeGrowthDefs", jsonout.json_list([])), jsonout.json_pair("statMods", build_stat_modifiers()), jsonout.json_pair("statModAppears", jsonout.json_obj([])), jsonout.json_pair("heroes", build_heroes()), jsonout.json_pair("blueprints", build_blueprints()), jsonout.json_pair("evoBlueprints", jsonout.json_obj([])), jsonout.json_pair("characters", build_characters()), jsonout.json_pair("synergyBonuses", jsonout.json_obj([])), jsonout.json_pair("attackValues", build_attack_values()), jsonout.json_pair("blueprintBonuses", jsonout.json_obj([])), jsonout.json_pair("heroClasses", build_hero_classes()), jsonout.json_pair("staminaRegen", jsonout.json_obj([])), jsonout.json_pair("rarityProperties", build_rarity_properties()), jsonout.json_pair("evoCosts", jsonout.json_obj([])), jsonout.json_pair("curves", jsonout.json_obj([]))])
end

public function build_hero_entry(bid: text, rank: integer, level: integer): Json
  intent: build
  let entry: RosterEntry = roster_entry(bid)
  let stats: Stats = base_stats(bid, rank, level)
  let rating: integer = rating_for(stats.hp, stats.atk)
  jsonout.json_obj([jsonout.json_pair("entity_type", jsonout.json_text("bot")), jsonout.json_pair("bid", jsonout.json_text(bid)), jsonout.json_pair("rank", jsonout.json_int(rank)), jsonout.json_pair("level", jsonout.json_int(level)), jsonout.json_pair("sig_lvl", jsonout.json_int(0)), jsonout.json_pair("required_xp", jsonout.json_int(0)), jsonout.json_pair("max_xp", jsonout.json_int(100)), jsonout.json_pair("stamina", jsonout.json_int(100)), jsonout.json_pair("stamina_ts", jsonout.json_int(0)), jsonout.json_pair("stamina_full_ts", jsonout.json_int(0)), jsonout.json_pair("stt", jsonout.json_text("")), jsonout.json_pair("max_hp", jsonout.json_int(stats.hp)), jsonout.json_pair("attack", jsonout.json_int(stats.atk)), jsonout.json_pair("rating", jsonout.json_int(rating)), jsonout.json_pair("rating_attack", jsonout.json_int(stats.atk / 2)), jsonout.json_pair("rating_hp", jsonout.json_int(stats.hp / 2)), jsonout.json_pair("rating_attack_base", jsonout.json_int(stats.atk / 2)), jsonout.json_pair("rating_hp_base", jsonout.json_int(stats.hp / 2)), jsonout.json_pair("special_attacks", jsonout.json_int(max_special_attacks(bid, entry.star))), jsonout.json_pair("pvpb", jsonout.json_obj([])), jsonout.json_pair("exc", jsonout.json_obj([])), jsonout.json_pair("mana_gain", jsonout.json_dec(mana_gain_rate())), jsonout.json_pair("mana_start", jsonout.json_int(diag_mana_start())), jsonout.json_pair("flvl", jsonout.json_int(0)), jsonout.json_pair("req_fxp", jsonout.json_int(0)), jsonout.json_pair("max_fxp", jsonout.json_int(0)), jsonout.json_pair("mfl", jsonout.json_int(0))])
end

public function default_team(): a list of text
  intent: return
  ["optimusprime_cin_tf", "optimusprimal_bw_mp32", "megatron_g1_mp10"]
end

public function boss_blueprint(): text
  intent: return
  "sharkticon_gs_brawler"
end

public function minion_blueprint(): text
  intent: return
  "sharkticon_gs_warrior"
end

public function quest_dim(): integer
  intent: return
  11
end

public function quest_path_col(): integer
  intent: return
  1
end

public function quest_has_encounter(row: integer): boolean
  intent: check
  row >= 1 and row <= quest_dim() - 1
end

public function quest_encounter(row: integer): QuestEncounter
  intent: get
  if row == 1 then
  QuestEncounter { key: minion_blueprint(), final_boss: false, label: "Patrol" }
else if row == 2 then
  QuestEncounter { key: "sharkticon_gs_scout", final_boss: false, label: "Scouting Party" }
else if row == 3 then
  QuestEncounter { key: "sharkticon_gs_tech", final_boss: false, label: "Signal Jammer" }
else if row == 4 then
  QuestEncounter { key: "sharkticon_gs_demolition", final_boss: false, label: "Demolition Crew" }
else if row == 5 then
  QuestEncounter { key: "sharkticon_gs_tactician", final_boss: false, label: "Ambush" }
else if row == 6 then
  QuestEncounter { key: "sharkticon_gs_kabam", final_boss: false, label: "Gilded Enforcer" }
else if row == 7 then
  QuestEncounter { key: "kickback_gs_kabam", final_boss: false, label: "Insecticon Raid" }
else if row == 8 then
  QuestEncounter { key: "waspinator_gs_deluxe", final_boss: false, label: "Buzzsaw Swarm" }
else if row == 9 then
  QuestEncounter { key: "soundwave_gs", final_boss: false, label: "Interceptor" }
else
  QuestEncounter { key: boss_blueprint(), final_boss: true, label: "Boss" }
end
end

function roster_has_bid(bid: text): boolean
  intent: find
  mutable found: boolean = false
  for entry in roster() do
    if entry.bid == bid then
  set found = true
end
  end
  found
end

public function resolve_team(team: a list of text): a list of text
  intent: return
  if length(team) == 0 then
  default_team()
else
  mutable valid: boolean = true
  for bid in team do
    if not roster_has_bid(bid) then
  set valid = false
end
  end
  if valid then
  team
else
  default_team()
end
end
end

function dump_slice2(): nothing
  intent: print
  mutable bases: a list of Pair = []
  mutable entries: a list of Pair = []
  for bid in ["optimusprime_cin_tf", "sharkticon_gs_brawler", "fte_optimus_gs_t3", "megatron_g1_mp10"] do
    for rank in [1, 2, 3, 5] do
      set bases = append(bases, jsonout.json_pair(bid ++ "/" ++ to_text(rank), build_hero_base(bid, rank)))
    end
    for rank in [1, 3] do
      for level in [1, 7, 30] do
        set entries = append(entries, jsonout.json_pair(bid ++ "/" ++ to_text(rank) ++ "/" ++ to_text(level), build_hero_entry(bid, rank, level)))
      end
    end
  end
  print(jsonout.encode_compact(jsonout.json_obj([jsonout.json_pair("characters", build_characters()), jsonout.json_pair("attack_values", build_attack_values()), jsonout.json_pair("missions_config", build_missions_config()), jsonout.json_pair("missions_autorefresh_result", build_missions_autorefresh_result()), jsonout.json_pair("missions_autorefresh_update", build_missions_autorefresh_update()), jsonout.json_pair("missions_account_data", build_missions_account_data()), jsonout.json_pair("hero_classes", build_hero_classes()), jsonout.json_pair("rarity_properties", build_rarity_properties()), jsonout.json_pair("stat_modifiers", build_stat_modifiers()), jsonout.json_pair("heroes", build_heroes()), jsonout.json_pair("login_data", build_login_data()), jsonout.json_pair("hero_base", jsonout.json_obj(bases)), jsonout.json_pair("hero_entry", jsonout.json_obj(entries))])))
end

function dump_teams(): nothing
  intent: print
  print("DEFAULT_TEAM " ++ join(default_team(), ","))
  print("BOSS_BLUEPRINT " ++ boss_blueprint())
  print("TEAM NONE -> " ++ join(resolve_team([]), ","))
  print("TEAM EMPTY -> " ++ join(resolve_team([]), ","))
  print("TEAM optimusprime_cin_tf -> " ++ join(resolve_team(["optimusprime_cin_tf"]), ","))
  print("TEAM megatron_g1_mp10,optimusprime_cin_tf -> " ++ join(resolve_team(["megatron_g1_mp10", "optimusprime_cin_tf"]), ","))
  print("TEAM no_such_bot_xyz -> " ++ join(resolve_team(["no_such_bot_xyz"]), ","))
  print("TEAM optimusprime_cin_tf,no_such_bot_xyz -> " ++ join(resolve_team(["optimusprime_cin_tf", "no_such_bot_xyz"]), ","))
  print("TEAM optimusprime_cin_tf,megatron_g1_mp10,optimusprimal_bw_mp32 -> " ++ join(resolve_team(["optimusprime_cin_tf", "megatron_g1_mp10", "optimusprimal_bw_mp32"]), ","))
end

function main(): nothing
  intent: run
  let arguments: a list of text = get_args()
  let mode: text = unwrap_or(get(arguments, 1), "")
  if mode == "" then
  build_responses()
  print("roster: " ++ to_text(length(roster())) ++ " bots, " ++ to_text(length(owned())) ++ " owned")
else if mode == "--dump-blueprints" then
  print(jsonout.encode_compact(build_blueprints()))
else if mode == "--dump-helpers" then
  dump_helpers()
else if mode == "--dump-slice2" then
  dump_slice2()
else if mode == "--dump-teams" then
  dump_teams()
else if mode == "--dump-slice3" then
  dump_slice3()
else if mode == "--dump-arena" then
  dump_arena()
else if mode == "--dump-slice4" then
  dump_slice4()
else if mode == "--dump-mana-start" then
  dump_mana_start()
else
  print("usage: gamedata.lbl --dump-blueprints|--dump-helpers|--dump-slice2|--dump-teams|--dump-slice3|--dump-arena|--dump-slice4|--dump-mana-start")
end
end

function is_digits(value: text): boolean
  intent: check
  mutable index: integer = 0
  mutable valid: boolean = text_length(value) > 0
  while index < text_length(value) do
    if not contains_text("0123456789", substring(value, index, 1)) then
  set valid = false
end
    set index = index + 1
  end
  valid
end

function arena_levels_json(): Json
  intent: build
  let indices: Json = jsonout.json_list([jsonout.json_int(0), jsonout.json_int(1), jsonout.json_int(2)])
  jsonout.json_obj([jsonout.json_pair("chicago", indices), jsonout.json_pair("hongkong", indices), jsonout.json_pair("karnak", indices), jsonout.json_pair("mine", indices), jsonout.json_pair("rust", indices)])
end

function is_arena_level(level: text): boolean
  intent: check
  level == "chicago" or level == "hongkong" or level == "karnak" or level == "mine" or level == "rust"
end

public function arena_level(): text
  intent: get
  let level: text = unwrap_or(env_get("TFTF_ARENA_LEVEL"), "chicago")
  if is_arena_level(level) then
  level
else
  print("[!] gamedata: Invalid TFTF_ARENA_LEVEL '" ++ level ++ "'; valid levels: chicago, hongkong, karnak, mine, rust")
  exit_process(1)
  ""
end
end

-- These exact names must match the AIProfile and AIPersonality assets shipped in the client. Live testing proved valid names alone do not make the offline opponent select the ranged branch.
public function enemy_ai_profile(): text
  intent: name
  "Default"
end

public function enemy_ai_personality(): text
  intent: name
  "Ranged"
end

public function arena_tod_index(): integer
  intent: get
  let level: text = arena_level()
  let raw: text = unwrap_or(env_get("TFTF_ARENA_TOD"), "0")
  let index: integer = to_integer(raw)
  if is_digits(raw) and index >= 0 and index <= 2 then
  index
else
  print("[!] gamedata: Invalid TFTF_ARENA_TOD " ++ raw ++ " for level '" ++ level ++ "'; valid indices: 0, 1, 2")
  exit_process(1)
  0
end
end

function vec2(x: integer, y: integer): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("x", jsonout.json_int(x)), jsonout.json_pair("y", jsonout.json_int(y))])
end

function json_hero_entries(bids: a list of text): a list of Json
  intent: build
  mutable entries: a list of Json = []
  for bid in bids do
    set entries = append(entries, build_hero_entry(bid, 1, 1))
  end
  entries
end

function json_hero_object(bids: a list of text): Json
  intent: build
  mutable entries: a list of Pair = []
  for bid in bids do
    set entries = append(entries, jsonout.json_pair(bid, build_hero_entry(bid, 1, 1)))
  end
  jsonout.json_obj(entries)
end

public function build_saved_team(team_id: text, heroes: a list of text): Json
  intent: build
  let bids: a list of text = resolve_team(heroes)
  jsonout.json_obj([jsonout.json_pair("sid", jsonout.json_text(team_id)), jsonout.json_pair("heroes", jsonout.json_list(json_hero_entries(bids)))])
end

public function build_active_team(activity_id: text, heroes: a list of text): Json
  intent: build
  let bids: a list of text = resolve_team(heroes)
  jsonout.json_obj([jsonout.json_pair("aid", jsonout.json_text(activity_id)), jsonout.json_pair("type", jsonout.json_text("PvE")), jsonout.json_pair("modes", jsonout.json_list([jsonout.json_text("PvE")])), jsonout.json_pair("heroes", json_hero_object(bids)), jsonout.json_pair("expire", jsonout.json_int(0))])
end

public function build_user_data(team: a list of text): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("userData", jsonout.json_obj([jsonout.json_pair("blueprintsMax", jsonout.json_int(500)), jsonout.json_pair("teamSizeMax", jsonout.json_int(3)), jsonout.json_pair("teamCountMax", jsonout.json_int(5))])), jsonout.json_pair("updates", jsonout.json_obj([jsonout.json_pair("heroes", jsonout.json_list(json_hero_entries(owned()))), jsonout.json_pair("savedTeams", jsonout.json_list([build_saved_team("0", team)])), jsonout.json_pair("activeTeams", jsonout.json_list([build_active_team("1.1.1-0", team)]))])), jsonout.json_pair("deletes", jsonout.json_obj([]))])
end

function mission_part(parts: a list of text, index: integer): integer
  intent: get
  let part: text = unwrap_or(get(parts, index), "")
  if is_digits(part) then
  to_integer(part)
else
  1
end
end

function dialogue_entry(in_shadow: boolean, character: text, side: text, style: text, line: text): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("inShadow", jsonout.json_bool(in_shadow)), jsonout.json_pair("character", jsonout.json_text(character)), jsonout.json_pair("side", jsonout.json_text(side)), jsonout.json_pair("style", jsonout.json_text(style)), jsonout.json_pair("line", jsonout.json_text(line))])
end

function dialogue_set(id: text, entries: a list of Json): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text(id)), jsonout.json_pair("entries", jsonout.json_list(entries))])
end

public function build_dialogue_sets(): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("arrival_intro", dialogue_set("arrival_intro", [dialogue_entry(false, "optimusprime_cin_tf", "left", "default", "Sensors read a cold beachhead ahead. Whatever landed here is already moving."), dialogue_entry(true, "sharkticon_gs_warrior", "right", "default", "The tide brought us in. The tide will bury you."), dialogue_entry(false, "optimusprime_cin_tf", "left", "default", "Then we hold this line, and we hold it together.")])), jsonout.json_pair("midpoint_rally", dialogue_set("midpoint_rally", [dialogue_entry(false, "optimusprimal_bw_mp32", "left", "default", "Halfway up the ridge and they still keep coming. This is a staging ground, not a patrol."), dialogue_entry(true, "soundwave_gs", "right", "default", "Every step you take is recorded. Every step you take is counted."), dialogue_entry(false, "optimusprime_cin_tf", "left", "default", "Then we walk it to the end. Steady spark, higher guard.")])), jsonout.json_pair("final_stand", dialogue_set("final_stand", [dialogue_entry(true, "sharkticon_gs_brawler", "right", "default", "You climbed a long way just to break at the top."), dialogue_entry(false, "optimusprime_cin_tf", "left", "default", "We climbed a long way to end this. Stand down or fall down.")])), jsonout.json_pair("shore_secured", dialogue_set("shore_secured", [dialogue_entry(false, "optimusprime_cin_tf", "left", "default", "The shore is quiet. Log the coordinates and get the wounded home."), dialogue_entry(false, "optimusprimal_bw_mp32", "left", "default", "Quiet today. We will be back before it gets loud again.")]))])
end

public function build_quest_summary(mission_id: text, set_id: text): Json
  intent: build
  let parts: a list of text = split(mission_id, ".")
  let act: integer = mission_part(parts, 0)
  let chapter: integer = mission_part(parts, 1)
  let mission: integer = mission_part(parts, 2)
  jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text(mission_id)), jsonout.json_pair("setId", jsonout.json_text(set_id)), jsonout.json_pair("hash", jsonout.json_text("h1")), jsonout.json_pair("act", jsonout.json_int(act)), jsonout.json_pair("chapter", jsonout.json_int(chapter)), jsonout.json_pair("mission", jsonout.json_int(mission)), jsonout.json_pair("missionIndex", jsonout.json_int(mission)), jsonout.json_pair("index", jsonout.json_int(mission)), jsonout.json_pair("friendlyName", jsonout.json_text("Arrival")), jsonout.json_pair("description", jsonout.json_text("The first battle.")), jsonout.json_pair("category", jsonout.json_text("story")), jsonout.json_pair("difficulty", jsonout.json_text("normal")), jsonout.json_pair("energyPerTile", jsonout.json_int(1)), jsonout.json_pair("minXpPerTile", jsonout.json_int(1)), jsonout.json_pair("maxXpPerTile", jsonout.json_int(2)), jsonout.json_pair("minHealthPerTile", jsonout.json_int(100)), jsonout.json_pair("maxHealthPerTile", jsonout.json_int(100)), jsonout.json_pair("image", jsonout.json_text("")), jsonout.json_pair("theme", jsonout.json_text("primordial")), jsonout.json_pair("todIndex", jsonout.json_int(0)), jsonout.json_pair("dialogueSets", build_dialogue_sets())])
end

public function build_quest_detail(mission_id: text, set_id: text): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("data", build_quest_summary(mission_id, set_id)), jsonout.json_pair("progression", jsonout.json_obj([]))])
end

function links_for(row: integer, col: integer): Json
  intent: build
  mutable links: a list of Json = []
  if col == 1 then
  if row - 1 >= 0 then
  set links = append(links, vec2(row - 1, 1))
end
  if row + 1 < quest_dim() then
  set links = append(links, vec2(row + 1, 1))
end
end
  jsonout.json_list(links)
end

function filler_tile(): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("walkable", jsonout.json_bool(false)), jsonout.json_pair("hidden", jsonout.json_bool(true))])
end

function start_tile(links: Json): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("walkable", jsonout.json_bool(true)), jsonout.json_pair("hidden", jsonout.json_bool(false)), jsonout.json_pair("start", jsonout.json_bool(true)), jsonout.json_pair("lab", jsonout.json_text("Start")), jsonout.json_pair("links", links), jsonout.json_pair("visibleLinks", links)])
end

function middle_tile(row: integer, links: Json): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("walkable", jsonout.json_bool(true)), jsonout.json_pair("hidden", jsonout.json_bool(false)), jsonout.json_pair("lab", jsonout.json_text("Node " ++ to_text(row))), jsonout.json_pair("links", links), jsonout.json_pair("visibleLinks", links)])
end

function encounter_tile(row: integer, links: Json): Json
  intent: build
  let encounter: QuestEncounter = quest_encounter(row)
  let enemy: Json = build_quest_enemy_for("", -1, encounter.key, encounter.final_boss)
  jsonout.json_obj([jsonout.json_pair("walkable", jsonout.json_bool(true)), jsonout.json_pair("hidden", jsonout.json_bool(false)), jsonout.json_pair("final", jsonout.json_bool(encounter.final_boss)), jsonout.json_pair("lab", jsonout.json_text(encounter.label)), jsonout.json_pair("links", links), jsonout.json_pair("visibleLinks", links), jsonout.json_pair("boss", jsonout.json_text(encounter.key)), jsonout.json_pair("entities", jsonout.json_obj([jsonout.json_pair(encounter.key, enemy)]))])
end

public function build_quest_map(qid: text): Json
  intent: build
  mutable grid: a list of Json = []
  mutable row: integer = 0
  while row < quest_dim() do
    mutable tiles: a list of Json = []
    mutable col: integer = 0
    while col < quest_dim() do
      if col != quest_path_col() then
  set tiles = append(tiles, filler_tile())
else
  let links: Json = links_for(row, col)
  if row == 0 then
  set tiles = append(tiles, start_tile(links))
else if quest_has_encounter(row) then
  set tiles = append(tiles, encounter_tile(row, links))
else
  set tiles = append(tiles, middle_tile(row, links))
end
end
      set col = col + 1
    end
    set grid = append(grid, jsonout.json_list(tiles))
    set row = row + 1
  end
  mutable path_points: a list of Json = []
  mutable path_row: integer = 0
  while path_row < quest_dim() do
    set path_points = append(path_points, vec2(path_row, quest_path_col()))
    set path_row = path_row + 1
  end
  let path: Json = jsonout.json_list(path_points)
  jsonout.json_obj([jsonout.json_pair("hash", jsonout.json_text("qm_" ++ qid)), jsonout.json_pair("v", jsonout.json_int(1)), jsonout.json_pair("mapHash", jsonout.json_text("qm_" ++ qid)), jsonout.json_pair("gridDimension", jsonout.json_int(quest_dim())), jsonout.json_pair("grid", jsonout.json_list(grid)), jsonout.json_pair("walkableCount", jsonout.json_int(quest_dim())), jsonout.json_pair("visibleWalkableCount", jsonout.json_int(quest_dim())), jsonout.json_pair("pathData", jsonout.json_list([jsonout.json_obj([jsonout.json_pair("path", path)])])), jsonout.json_pair("overrideZoom", jsonout.json_int(0))])
end

public function build_quest_enemy_for(map_override: text, tod_index: integer, key: text, is_final_boss: boolean): Json
  intent: build
  let selected_map: text = if map_override == "" then
  arena_level()
else
  map_override
end
  let selected_tod: integer = if tod_index < 0 then
  arena_tod_index()
else
  tod_index
end
  jsonout.json_obj([jsonout.json_pair("key", jsonout.json_text(key)), jsonout.json_pair("entityType", jsonout.json_text("boss")), jsonout.json_pair("parentEntityType", jsonout.json_text("boss")), jsonout.json_pair("isFinalBoss", jsonout.json_bool(is_final_boss)), jsonout.json_pair("characters", jsonout.json_list([jsonout.json_text(key)])), jsonout.json_pair("rank", jsonout.json_int(1)), jsonout.json_pair("level", jsonout.json_int(1)), jsonout.json_pair("sig_lvl", jsonout.json_int(0)), jsonout.json_pair("flvl", jsonout.json_int(0)), jsonout.json_pair("aiType", jsonout.json_int(0)), jsonout.json_pair("aiString", jsonout.json_text(enemy_ai_profile())), jsonout.json_pair("aiPer", jsonout.json_text(enemy_ai_personality())), jsonout.json_pair("mapOverride", jsonout.json_text(selected_map)), jsonout.json_pair("todIndex", jsonout.json_int(selected_tod))])
end

public function build_quest_enemy(map_override: text, tod_index: integer): Json
  intent: build
  build_quest_enemy_for(map_override, tod_index, boss_blueprint(), true)
end

public function local_uid(): text
  intent: return
  "1000000000001"
end

function quest_team(bids: a list of text): Json
  intent: build
  mutable entries: a list of Pair = []
  for bid in bids do
    let stats: Stats = base_stats(bid, 1, 1)
    let pi: integer = to_integer(to_decimal(stats.hp + stats.atk) / 20)
    let hero: Json = jsonout.json_obj([jsonout.json_pair("hp", jsonout.json_dec(1)), jsonout.json_pair("pi", jsonout.json_int(pi)), jsonout.json_pair("sig_lvl", jsonout.json_int(0)), jsonout.json_pair("stat_mods", jsonout.json_list([])), jsonout.json_pair("sig_mods", jsonout.json_list([]))])
    set entries = append(entries, jsonout.json_pair(bid, hero))
  end
  jsonout.json_obj(entries)
end

function revealed_tiles(): Json
  intent: build
  mutable tiles: a list of Json = []
  mutable row: integer = 0
  while row < quest_dim() do
    set tiles = append(tiles, vec2(row, quest_path_col()))
    set row = row + 1
  end
  jsonout.json_list(tiles)
end

public function build_quest_progression(qid: text, start_x: integer, start_y: integer, team: a list of text): Json
  intent: build
  let bids: a list of text = resolve_team(team)
  let user: Json = jsonout.json_obj([jsonout.json_pair("name", jsonout.json_text("Commander")), jsonout.json_pair("tag", jsonout.json_text("")), jsonout.json_pair("strongestHero", jsonout.json_text(unwrap_or(get(bids, 0), ""))), jsonout.json_pair("currentPos", vec2(start_x, start_y)), jsonout.json_pair("team", quest_team(bids)), jsonout.json_pair("points", jsonout.json_int(0))])
  jsonout.json_obj([jsonout.json_pair("version", jsonout.json_int(1)), jsonout.json_pair("currentPos", vec2(start_x, start_y)), jsonout.json_pair("cleared", jsonout.json_list([])), jsonout.json_pair("previouslyCleared", jsonout.json_list([])), jsonout.json_pair("revealed", revealed_tiles()), jsonout.json_pair("users", jsonout.json_obj([jsonout.json_pair(local_uid(), user)]))])
end

public function build_active_quest(qid: text, set_id: text, team: a list of text): Json
  intent: build
  let qmap: Json = build_quest_map(qid)
  let progression: Json = build_quest_progression(qid, 0, 1, team)
  let base_instance: Json = jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text(qid)), jsonout.json_pair("qid", jsonout.json_text(qid)), jsonout.json_pair("index", jsonout.json_int(0)), jsonout.json_pair("instanceIndex", jsonout.json_int(0)), jsonout.json_pair("phase", jsonout.json_int(0)), jsonout.json_pair("startTime", jsonout.json_int(0)), jsonout.json_pair("expiryTime", jsonout.json_int(0)), jsonout.json_pair("data", build_quest_summary(qid, set_id)), jsonout.json_pair("map", qmap)])
  let nested_instance: Json = jsonout.obj_set(base_instance, "progression", progression)
  let instance: Json = jsonout.obj_set(jsonout.obj_set(jsonout.obj_set(jsonout.obj_set(jsonout.obj_set(jsonout.obj_set(nested_instance, "version", jsonout.obj_get(progression, "version")), "currentPos", jsonout.obj_get(progression, "currentPos")), "cleared", jsonout.obj_get(progression, "cleared")), "previouslyCleared", jsonout.obj_get(progression, "previouslyCleared")), "revealed", jsonout.obj_get(progression, "revealed")), "users", jsonout.obj_get(progression, "users"))
  jsonout.json_obj([jsonout.json_pair("uniqueId", jsonout.json_text(qid)), jsonout.json_pair("qid", jsonout.json_text(qid)), jsonout.json_pair("id", jsonout.json_text(qid)), jsonout.json_pair("category", jsonout.json_text("PvE")), jsonout.json_pair("mode", jsonout.json_text("PvE")), jsonout.json_pair("setId", jsonout.json_text(set_id)), jsonout.json_pair("hash", jsonout.json_text("h1")), jsonout.json_pair("phase", jsonout.json_int(0)), jsonout.json_pair("data", build_quest_summary(qid, set_id)), jsonout.json_pair("map", qmap), jsonout.json_pair("progression", jsonout.json_obj([])), jsonout.json_pair("instances", jsonout.json_list([instance]))])
end

public function build_quest_begin(qid: text, set_id: text, team: a list of text): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("activeQuests", jsonout.json_obj([jsonout.json_pair(qid, build_active_quest(qid, set_id, team))]))])
end

function battle_user(user: Json, x: integer, y: integer, key: text): Json
  intent: build
  let with_id: Json = jsonout.obj_set(user, "currentBattleId", jsonout.json_text(key))
  let with_pos: Json = jsonout.obj_set(with_id, "currentBattlePos", vec2(x, y))
  jsonout.obj_set(with_pos, "currentBattleState", jsonout.json_text("activated"))
end

public function build_quest_movedir(qid: text, offx: integer, offy: integer, start_x: integer, start_y: integer, team: a list of text): Json
  intent: build
  let next_x: integer = start_x + offx
  let next_y: integer = start_y + offy
  mutable results: a list of Json = [jsonout.json_obj([jsonout.json_pair("action", jsonout.json_obj([jsonout.json_pair("moveto", vec2(next_x, next_y))]))])]
  if next_y == quest_path_col() and quest_has_encounter(next_x) then
  let encounter: QuestEncounter = quest_encounter(next_x)
  let battle: Json = jsonout.json_obj([jsonout.json_pair("x", jsonout.json_int(next_x)), jsonout.json_pair("y", jsonout.json_int(next_y)), jsonout.json_pair("isFinalBoss", jsonout.json_bool(encounter.final_boss)), jsonout.json_pair("battleEnemy", build_quest_enemy_for("", -1, encounter.key, encounter.final_boss))])
  set results = append(results, jsonout.json_obj([jsonout.json_pair("action", jsonout.json_obj([jsonout.json_pair("battle", battle)]))]))
end
  let initial: Json = build_quest_progression(qid, next_x, next_y, team)
  let with_cleared: Json = jsonout.obj_set(initial, "cleared", jsonout.json_list([vec2(start_x, start_y)]))
  let progression: Json = jsonout.obj_set(with_cleared, "revealed", revealed_tiles())
  let final_progression: Json = if next_y == quest_path_col() and quest_has_encounter(next_x) then
  let encounter: QuestEncounter = quest_encounter(next_x)
  let with_id: Json = jsonout.obj_set(progression, "currentBattleId", jsonout.json_text(encounter.key))
  let with_pos: Json = jsonout.obj_set(with_id, "currentBattlePos", vec2(next_x, next_y))
  let with_enemy: Json = jsonout.obj_set(with_pos, "currentBattleEnemy", jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text(encounter.key))]))
  let with_health: Json = jsonout.obj_set(with_enemy, "currentBattleEnemyHealth", jsonout.json_dec(1))
  let users: Json = jsonout.obj_get(with_health, "users")
  jsonout.obj_set(with_health, "users", jsonout.obj_set(users, local_uid(), battle_user(jsonout.obj_get(users, local_uid()), next_x, next_y, encounter.key)))
else
  progression
end
  let team_data: Json = build_active_team(qid ++ "-0", resolve_team(team))
  jsonout.json_obj([jsonout.json_pair("results", jsonout.json_list(results)), jsonout.json_pair("progression", final_progression), jsonout.json_pair("teamData", team_data)])
end

function dump_slice3(): nothing
  intent: print
  let team: a list of text = ["grimlock_gs_mp08", "megatron_cin_rotf"]
  print(jsonout.encode_compact(jsonout.json_obj([jsonout.json_pair("saved_team_default", build_saved_team("0", [])), jsonout.json_pair("saved_team_custom", build_saved_team("7", team)), jsonout.json_pair("active_team_default", build_active_team("1.1.1-0", [])), jsonout.json_pair("active_team_custom", build_active_team("1.1.1-0", team)), jsonout.json_pair("user_data_default", build_user_data([])), jsonout.json_pair("user_data_custom", build_user_data(team)), jsonout.json_pair("quest_summary_default", build_quest_summary("1.1.1", "story_act1")), jsonout.json_pair("quest_summary_alt", build_quest_summary("2.3.4", "story_act2")), jsonout.json_pair("quest_detail_default", build_quest_detail("1.1.1", "story_act1")), jsonout.json_pair("quest_detail_alt", build_quest_detail("2.3.4", "story_act2")), jsonout.json_pair("quest_map_default", build_quest_map("1.1.1")), jsonout.json_pair("quest_map_alt", build_quest_map("2.3.4")), jsonout.json_pair("quest_enemy_default", build_quest_enemy("", -1)), jsonout.json_pair("quest_enemy_override", build_quest_enemy("mine", 2)), jsonout.json_pair("quest_progression_default", build_quest_progression("1.1.1", 0, 1, [])), jsonout.json_pair("quest_progression_start21", build_quest_progression("1.1.1", 2, 1, [])), jsonout.json_pair("quest_progression_custom", build_quest_progression("1.1.1", 0, 1, team)), jsonout.json_pair("quest_progression_unknown", build_quest_progression("1.1.1", 0, 1, ["not-a-blueprint"])), jsonout.json_pair("active_quest_default", build_active_quest("1.1.1", "story_act1", [])), jsonout.json_pair("active_quest_custom", build_active_quest("1.1.1", "story_act1", team)), jsonout.json_pair("quest_begin_default", build_quest_begin("1.1.1", "story_act1", [])), jsonout.json_pair("quest_begin_custom", build_quest_begin("1.1.1", "story_act1", team)), jsonout.json_pair("quest_movedir_default", build_quest_movedir("1.1.1", 1, 0, 0, 1, [])), jsonout.json_pair("quest_movedir_to_boss", build_quest_movedir("1.1.1", 1, 0, 1, 1, [])), jsonout.json_pair("quest_movedir_to_boss_custom", build_quest_movedir("1.1.1", 1, 0, 1, 1, team)), jsonout.json_pair("quest_movedir_west", build_quest_movedir("1.1.1", -1, 0, 1, 1, [])), jsonout.json_pair("local_uid", jsonout.json_text(local_uid())), jsonout.json_pair("arena_level", jsonout.json_text(arena_level())), jsonout.json_pair("arena_tod", jsonout.json_int(arena_tod_index())), jsonout.json_pair("arena_levels", arena_levels_json())])))
end

function dump_arena(): nothing
  intent: print
  let enemy: Json = build_quest_enemy("", -1)
  let map: Json = build_quest_map("1.1.1")
  let row: Json = unwrap_or(get(jsonout.list_items(jsonout.obj_get(map, "grid")), quest_dim() - 1), jsonout.json_list([]))
  let tile: Json = unwrap_or(get(jsonout.list_items(row), 1), jsonout.json_obj([]))
  let boss: Json = jsonout.obj_get(jsonout.obj_get(tile, "entities"), boss_blueprint())
  print("ARENA_LEVEL " ++ arena_level())
  print("ARENA_TOD " ++ to_text(arena_tod_index()))
  print("ENEMY_MAPOVERRIDE " ++ jsonout.as_text(jsonout.obj_get(enemy, "mapOverride")))
  print("ENEMY_TOD " ++ to_text(jsonout.as_int(jsonout.obj_get(enemy, "todIndex"))))
  print("BOSSTILE_MAPOVERRIDE " ++ jsonout.as_text(jsonout.obj_get(boss, "mapOverride")))
  print("BOSSTILE_TOD " ++ to_text(jsonout.as_int(jsonout.obj_get(boss, "todIndex"))))
end

public function base_id(): text
  intent: return
  "user_base_1"
end

public function base_theme(): text
  intent: return
  "primordial"
end

public function base_dim(): integer
  intent: return
  5
end

public function base_socket_type(): text
  intent: return
  "building"
end

function base_buildings(): a list of BuildingSpec
  intent: build
  [BuildingSpec { bid: "bldg_battle_centre", name: "Battle Centre", desc: "Coordinate your Autobots' battles from here.", model: "z_bldg_battle_centre_01" }, BuildingSpec { bid: "bldg_away_team", name: "Away Team", desc: "Send bots on away missions across Cybertron.", model: "z_bldg_away_team_01" }, BuildingSpec { bid: "bldg_alliance_help", name: "Alliance Beacon", desc: "Call in help from allied Autobots.", model: "z_bldg_alliance_help_01" }, BuildingSpec { bid: "bldg_crystal_free", name: "Crystal Chamber", desc: "Synthesizes a free crystal now and then.", model: "z_bldg_gacha_free_01" }, BuildingSpec { bid: "bldg_crystal_daily", name: "Daily Crystal Vault", desc: "Produces one crystal every day.", model: "z_bldg_gacha_daily_01" }, BuildingSpec { bid: "bldg_crystal_premium", name: "Premium Crystal Vault", desc: "Stores premium crystals.", model: "z_bldg_gacha_other_01" }]
end

function base_placements(): a list of BasePlacement
  intent: build
  [BasePlacement { row: 2, col: 2, bid: "bldg_battle_centre" }, BasePlacement { row: 2, col: 1, bid: "bldg_away_team" }, BasePlacement { row: 2, col: 3, bid: "bldg_alliance_help" }, BasePlacement { row: 1, col: 2, bid: "bldg_crystal_free" }, BasePlacement { row: 3, col: 2, bid: "bldg_crystal_daily" }]
end

public function build_base_summary(): Json
  intent: build
  jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text(base_id())), jsonout.json_pair("setId", jsonout.json_text("")), jsonout.json_pair("hash", jsonout.json_text("b1")), jsonout.json_pair("category", jsonout.json_text("base")), jsonout.json_pair("friendlyName", jsonout.json_text("Base")), jsonout.json_pair("description", jsonout.json_text("Your base.")), jsonout.json_pair("energyPerTile", jsonout.json_int(0)), jsonout.json_pair("minXpPerTile", jsonout.json_int(0)), jsonout.json_pair("maxXpPerTile", jsonout.json_int(0)), jsonout.json_pair("minHealthPerTile", jsonout.json_int(0)), jsonout.json_pair("maxHealthPerTile", jsonout.json_int(0)), jsonout.json_pair("image", jsonout.json_text("")), jsonout.json_pair("theme", jsonout.json_text(base_theme())), jsonout.json_pair("todIndex", jsonout.json_int(0))])
end

public function base_is_walkable(row: integer, col: integer): boolean
  intent: check
  let centre: integer = to_integer(to_decimal(base_dim()) / 2)
  row == centre or col == centre
end

public function base_socket_id(row: integer, col: integer): text
  intent: build
  "sock_" ++ to_text(row) ++ "_" ++ to_text(col)
end

function base_links_for(row: integer, col: integer): Json
  intent: build
  let dim: integer = base_dim()
  mutable links: a list of Json = []
  if row - 1 >= 0 and base_is_walkable(row - 1, col) then
  set links = append(links, vec2(row - 1, col))
end
  if row + 1 < dim and base_is_walkable(row + 1, col) then
  set links = append(links, vec2(row + 1, col))
end
  if col - 1 >= 0 and base_is_walkable(row, col - 1) then
  set links = append(links, vec2(row, col - 1))
end
  if col + 1 < dim and base_is_walkable(row, col + 1) then
  set links = append(links, vec2(row, col + 1))
end
  jsonout.json_list(links)
end

function base_tile(row: integer, col: integer, centre: integer): Json
  intent: build
  if not base_is_walkable(row, col) then
  jsonout.json_obj([jsonout.json_pair("walkable", jsonout.json_bool(false)), jsonout.json_pair("hidden", jsonout.json_bool(true))])
else
  let links: Json = base_links_for(row, col)
  let sockets: Json = jsonout.json_obj([jsonout.json_pair(base_socket_id(row, col), jsonout.json_obj([jsonout.json_pair("entityType", jsonout.json_text(base_socket_type())), jsonout.json_pair("locked", jsonout.json_bool(false))]))])
  let tile: Json = jsonout.json_obj([jsonout.json_pair("walkable", jsonout.json_bool(true)), jsonout.json_pair("hidden", jsonout.json_bool(false)), jsonout.json_pair("lab", jsonout.json_text("Plot " ++ to_text(row) ++ "-" ++ to_text(col))), jsonout.json_pair("links", links), jsonout.json_pair("visibleLinks", links), jsonout.json_pair("sockets", sockets)])
  if row == centre and col == centre then
  jsonout.obj_set(jsonout.obj_set(tile, "start", jsonout.json_bool(true)), "lab", jsonout.json_text("Command Centre"))
else
  tile
end
end
end

public function build_base_map(): Json
  intent: build
  let dim: integer = base_dim()
  let centre: integer = to_integer(to_decimal(dim) / 2)
  mutable grid: a list of Json = []
  mutable row: integer = 0
  while row < dim do
    mutable tiles: a list of Json = []
    mutable col: integer = 0
    while col < dim do
      set tiles = append(tiles, base_tile(row, col, centre))
      set col = col + 1
    end
    set grid = append(grid, jsonout.json_list(tiles))
    set row = row + 1
  end
  mutable walkable: integer = 0
  set row = 0
  while row < dim do
    mutable col: integer = 0
    while col < dim do
      if base_is_walkable(row, col) then
  set walkable = walkable + 1
end
      set col = col + 1
    end
    set row = row + 1
  end
  mutable horizontal: a list of Json = []
  mutable vertical: a list of Json = []
  mutable index: integer = 0
  while index < dim do
    set horizontal = append(horizontal, vec2(centre, index))
    set vertical = append(vertical, vec2(index, centre))
    set index = index + 1
  end
  let map_hash: text = "bm_" ++ base_id()
  jsonout.json_obj([jsonout.json_pair("hash", jsonout.json_text(map_hash)), jsonout.json_pair("v", jsonout.json_int(1)), jsonout.json_pair("mapHash", jsonout.json_text(map_hash)), jsonout.json_pair("gridDimension", jsonout.json_int(dim)), jsonout.json_pair("grid", jsonout.json_list(grid)), jsonout.json_pair("walkableCount", jsonout.json_int(walkable)), jsonout.json_pair("visibleWalkableCount", jsonout.json_int(walkable)), jsonout.json_pair("pathData", jsonout.json_list([jsonout.json_obj([jsonout.json_pair("path", jsonout.json_list(horizontal))]), jsonout.json_obj([jsonout.json_pair("path", jsonout.json_list(vertical))])])), jsonout.json_pair("overrideZoom", jsonout.json_int(0))])
end

public function build_base_mission(): Json
  intent: build
  mutable placements: a list of Pair = []
  for placement in base_placements() do
    let value: Json = jsonout.json_obj([jsonout.json_pair("entityType", jsonout.json_text("building")), jsonout.json_pair("parentEntityType", jsonout.json_text("building")), jsonout.json_pair("key", jsonout.json_text(placement.bid)), jsonout.json_pair("position", vec2(placement.row, placement.col))])
    set placements = append(placements, jsonout.json_pair(base_socket_id(placement.row, placement.col), value))
  end
  jsonout.json_obj([jsonout.json_pair("mode", jsonout.json_text("base")), jsonout.json_pair("category", jsonout.json_text("base")), jsonout.json_pair("id", jsonout.json_text(base_id())), jsonout.json_pair("hash", jsonout.json_text("b1")), jsonout.json_pair("setName", jsonout.json_text("")), jsonout.json_pair("setId", jsonout.json_text("")), jsonout.json_pair("uid", jsonout.json_text(local_uid())), jsonout.json_pair("aid", jsonout.json_text("")), jsonout.json_pair("modes", jsonout.json_list([jsonout.json_text("base")])), jsonout.json_pair("data", build_base_summary()), jsonout.json_pair("map", build_base_map()), jsonout.json_pair("placements", jsonout.json_obj(placements))])
end

public function build_base_available_buildings(): Json
  intent: build
  mutable buildings: a list of Json = []
  for spec in base_buildings() do
    let funds: Json = jsonout.json_obj([jsonout.json_pair("upgrade", jsonout.json_list([])), jsonout.json_pair("repair", jsonout.json_list([]))])
    let building: Json = jsonout.json_obj([jsonout.json_pair("entityType", jsonout.json_text("building")), jsonout.json_pair("id", jsonout.json_text(spec.bid)), jsonout.json_pair("name", jsonout.json_text(spec.name)), jsonout.json_pair("description", jsonout.json_text(spec.desc)), jsonout.json_pair("img", jsonout.json_text("")), jsonout.json_pair("modelId", jsonout.json_text(spec.model)), jsonout.json_pair("maxDamage", jsonout.json_int(100)), jsonout.json_pair("cost", jsonout.json_list([])), jsonout.json_pair("comingSoon", jsonout.json_bool(false)), jsonout.json_pair("levels", jsonout.json_list([])), jsonout.json_pair("rank", jsonout.json_int(1)), jsonout.json_pair("level", jsonout.json_int(1)), jsonout.json_pair("damage", jsonout.json_int(0)), jsonout.json_pair("funds", funds)])
    set buildings = append(buildings, building)
  end
  jsonout.json_list(buildings)
end

public function build_base_active(): Json
  intent: build
  mutable buildings: a list of Json = []
  for placement in base_placements() do
    set buildings = append(buildings, jsonout.json_obj([jsonout.json_pair("id", jsonout.json_text(placement.bid)), jsonout.json_pair("key", jsonout.json_text(base_socket_id(placement.row, placement.col)))]))
  end
  let dim: integer = base_dim()
  mutable sockets: a list of Pair = []
  mutable row: integer = 0
  while row < dim do
    mutable col: integer = 0
    while col < dim do
      if base_is_walkable(row, col) then
  set sockets = append(sockets, jsonout.json_pair(base_socket_id(row, col), jsonout.json_bool(true)))
end
      set col = col + 1
    end
    set row = row + 1
  end
  jsonout.json_obj([jsonout.json_pair("userBase", build_base_mission()), jsonout.json_pair("userAvailableBuildings", build_base_available_buildings()), jsonout.json_pair("userBuildings", jsonout.json_list(buildings)), jsonout.json_pair("userSockets", jsonout.json_obj(sockets))])
end

function hero_bid(hero: Json): text
  intent: get
  if jsonout.obj_has(hero, "bid") and not jsonout.is_null(jsonout.obj_get(hero, "bid")) then
  jsonout.as_text(jsonout.obj_get(hero, "bid"))
else
  ""
end
end

function hero_int(hero: Json, key: text, fallback: integer): integer
  intent: get
  if jsonout.obj_has(hero, key) and not jsonout.is_null(jsonout.obj_get(hero, key)) then
  let value: integer = jsonout.as_int(jsonout.obj_get(hero, key))
  if value == 0 then
  fallback
else
  value
end
else
  fallback
end
end

public function build_base_hero_details(req_heroes: a list of Json): Json
  intent: build
  mutable details: a list of Json = []
  for hero in req_heroes do
    let bid: text = hero_bid(hero)
    let rank: integer = hero_int(hero, "rank", 1)
    let level: integer = hero_int(hero, "level", 1)
    let sig_lvl: integer = hero_int(hero, "sig_lvl", 0)
    let entry: RosterEntry = roster_entry(bid)
    let stats: Stats = base_stats(bid, rank, level)
    let detail: Json = jsonout.json_obj([jsonout.json_pair("bid", jsonout.json_text(bid)), jsonout.json_pair("rank", jsonout.json_int(rank)), jsonout.json_pair("level", jsonout.json_int(level)), jsonout.json_pair("sig_lvl", jsonout.json_int(sig_lvl)), jsonout.json_pair("i", jsonout.json_text(art_base(bid))), jsonout.json_pair("img", jsonout.json_text(art_base(bid))), jsonout.json_pair("m", jsonout.json_text(model_id(bid))), jsonout.json_pair("mdl", jsonout.json_text(model_id(bid))), jsonout.json_pair("rating_hp", jsonout.json_int(stats.hp)), jsonout.json_pair("max_hp", jsonout.json_int(stats.hp)), jsonout.json_pair("rating_attack", jsonout.json_int(stats.atk)), jsonout.json_pair("attack", jsonout.json_int(stats.atk)), jsonout.json_pair("health", jsonout.json_int(stats.hp)), jsonout.json_pair("armor", jsonout.json_int(0)), jsonout.json_pair("crit_rate", jsonout.json_int(0)), jsonout.json_pair("crit_dmg", jsonout.json_int(0)), jsonout.json_pair("block_prof", jsonout.json_int(0)), jsonout.json_pair("perfect_block", jsonout.json_int(0)), jsonout.json_pair("sig_ability", jsonout.json_int(0)), jsonout.json_pair("special_attacks", jsonout.json_int(max_special_attacks(bid, entry.star))), jsonout.json_pair("user_owned", jsonout.json_bool(true)), jsonout.json_pair("mana_gain", jsonout.json_dec(mana_gain_rate())), jsonout.json_pair("mana_start", jsonout.json_int(diag_mana_start())), jsonout.json_pair("synergyBonuses", jsonout.json_list([])), jsonout.json_pair("pvpb", jsonout.json_obj([]))])
    set details = append(details, detail)
  end
  jsonout.json_list(details)
end

function response_envelope(result: Json): text
  intent: build
  jsonout.encode_compact(jsonout.json_obj([jsonout.json_pair("error", jsonout.json_null()), jsonout.json_pair("result", result)]))
end

public function build_responses(): nothing
  intent: write
  let login_name: text = "GET__bcg_getLoginData.json"
  write_file(path_join(resp_dir(), login_name), response_envelope(build_login_data()))
  print("wrote " ++ login_name ++ "  (" ++ to_text(length(roster())) ++ " entries)")
  let user_name: text = "GET__bcg_getUserData.json"
  write_file(path_join(resp_dir(), user_name), response_envelope(build_user_data([])))
  print("wrote " ++ user_name ++ "  (" ++ to_text(length(owned())) ++ " entries)")
  let account_path: text = path_join(resp_dir(), "GET__account_data.json")
  let account: Json = jsonout.parse_json(read_file(account_path))
  let result: Json = jsonout.obj_get(account, "result")
  let updated_result: Json = jsonout.obj_set(result, "missionsconfig", build_missions_account_data())
  write_file(account_path, jsonout.encode_compact(jsonout.obj_set(account, "result", updated_result)))
  print("updated GET__account_data.json missionsconfig")
  let refresh_name: text = "GET__autorefresh_missionsconfig_refresh.json"
  write_file(path_join(resp_dir(), refresh_name), response_envelope(build_missions_autorefresh_result()))
  print("wrote " ++ refresh_name)
end

function base_socket_ids_json(): Json
  intent: build
  mutable ids: a list of Json = []
  mutable row: integer = 0
  while row < base_dim() do
    mutable col: integer = 0
    while col < base_dim() do
      set ids = append(ids, jsonout.json_text(base_socket_id(row, col)))
      set col = col + 1
    end
    set row = row + 1
  end
  jsonout.json_list(ids)
end

function base_walkable_json(): Json
  intent: build
  mutable rows: a list of Json = []
  mutable row: integer = 0
  while row < base_dim() do
    mutable values: a list of Json = []
    mutable col: integer = 0
    while col < base_dim() do
      set values = append(values, jsonout.json_bool(base_is_walkable(row, col)))
      set col = col + 1
    end
    set rows = append(rows, jsonout.json_list(values))
    set row = row + 1
  end
  jsonout.json_list(rows)
end

function dump_slice4(): nothing
  intent: print
  let team: a list of text = default_team()
  let default_details: Json = build_base_hero_details([jsonout.json_obj([jsonout.json_pair("bid", jsonout.json_text(unwrap_or(get(team, 0), "")))]), jsonout.json_obj([jsonout.json_pair("bid", jsonout.json_text(unwrap_or(get(team, 1), "")))])])
  let full_details: Json = build_base_hero_details([jsonout.json_obj([jsonout.json_pair("bid", jsonout.json_text("fte_optimus_gs_t3")), jsonout.json_pair("rank", jsonout.json_int(3)), jsonout.json_pair("level", jsonout.json_int(30)), jsonout.json_pair("sig_lvl", jsonout.json_int(5))]), jsonout.json_obj([jsonout.json_pair("bid", jsonout.json_text("sharkticon_gs_brawler")), jsonout.json_pair("rank", jsonout.json_int(1)), jsonout.json_pair("level", jsonout.json_int(1)), jsonout.json_pair("sig_lvl", jsonout.json_int(0))])])
  let zero_details: Json = build_base_hero_details([jsonout.json_obj([jsonout.json_pair("bid", jsonout.json_text("megatron_g1_mp10")), jsonout.json_pair("rank", jsonout.json_int(0)), jsonout.json_pair("level", jsonout.json_int(0)), jsonout.json_pair("sig_lvl", jsonout.json_int(0))])])
  let unknown_details: Json = build_base_hero_details([jsonout.json_obj([jsonout.json_pair("bid", jsonout.json_text("not_a_bot_xyz"))])])
  print(jsonout.encode_compact(jsonout.json_obj([jsonout.json_pair("base_id", jsonout.json_text(base_id())), jsonout.json_pair("base_theme", jsonout.json_text(base_theme())), jsonout.json_pair("base_dim", jsonout.json_int(base_dim())), jsonout.json_pair("base_socket_type", jsonout.json_text(base_socket_type())), jsonout.json_pair("base_socket_ids", base_socket_ids_json()), jsonout.json_pair("base_walkable", base_walkable_json()), jsonout.json_pair("base_summary", build_base_summary()), jsonout.json_pair("base_map", build_base_map()), jsonout.json_pair("base_mission", build_base_mission()), jsonout.json_pair("base_available_buildings", build_base_available_buildings()), jsonout.json_pair("base_active", build_base_active()), jsonout.json_pair("base_hero_details_empty", build_base_hero_details([])), jsonout.json_pair("base_hero_details_default", default_details), jsonout.json_pair("base_hero_details_full", full_details), jsonout.json_pair("base_hero_details_zero_fallbacks", zero_details), jsonout.json_pair("base_hero_details_unknown", unknown_details), jsonout.json_pair("login_data_envelope_len", jsonout.json_int(text_length(response_envelope(build_login_data())))), jsonout.json_pair("roster_count", jsonout.json_int(length(roster()))), jsonout.json_pair("owned_count", jsonout.json_int(length(owned())))])))
end

function dump_mana_start(): nothing
  intent: print
  let base: Json = build_hero_base(unwrap_or(get(default_team(), 0), ""), 1)
  print("MANA_START " ++ jsonout.encode_compact(jsonout.obj_get(base, "mana_start")))
end

main()

-- Preserve Python's left-to-right floating-point evaluation order for consumers
-- whose serialized bytes must match the Python payload exactly.
public function payload_base_stats(bid: text, rank: integer, level: integer): Stats
  intent: build
  let entry: RosterEntry = roster_entry(bid)
  let rank_mult: decimal = 1 + 0.35 * to_decimal(max(1, rank) - 1)
  let level_factor: decimal = 1 + to_decimal(max(1, level) - 1) / 100
  let hp_classed: decimal = to_decimal(star_hp(entry.star)) * class_hp_mult(entry.klass)
  let hp_ranked: decimal = hp_classed * rank_mult
  let atk_classed: decimal = to_decimal(star_atk(entry.star)) * class_atk_mult(entry.klass)
  let atk_ranked: decimal = atk_classed * rank_mult
  Stats { hp: to_integer(hp_ranked * level_factor), atk: to_integer(atk_ranked * level_factor) }
end
